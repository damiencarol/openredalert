/**
 * 
 */
package openra.tests;

import static org.junit.Assert.*;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.util.prefs.BackingStoreException;

import org.junit.Test;

/**
 * @author D.Carol
 *
 */
public class ORAIniFile {

	/**
	 * Test method for {@link openra.core.ORAIniFile#ORAIniFile(java.lang.String)}.
	 */
	@Test
	public void testORAIniFile() {
		/*openra.core.ORAIniFile tests = null;
		String[] keysMission;
		
		try {
			BufferedReader r = new BufferedReader(
			 new InputStreamReader(
			getClass().getResourceAsStream("openra.jar!/data/general/mission.ini") ));

		 tests = new openra.core.ORAIniFile("/data/general/mission.ini");	
		} catch (Exception e) {
			fail("Impossible to load \"main\\general\\mission.ini\" file.");
		}
		
		try {
			assertEquals(tests.keys().length, 0);
		
		
			keysMission	 = tests.keys();
		} catch (Exception e) {
			fail("Impossible to load ini file list in \"main\\general\\mission.ini\" file.");
		}
		*/
		
		
	}

}
